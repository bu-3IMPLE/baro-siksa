<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    tools:context=".activity.RestaurantFormActivity">

    <LinearLayout
        android:id="@+id/top_bar"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingHorizontal="8dp"
        app:layout_constraintTop_toTopOf="parent">

        <ImageView
            android:id="@+id/btn_back"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:clickable="true"
            android:focusable="true"
            android:padding="8dp"
            android:src="@drawable/ic_arrow_back"
            android:contentDescription="뒤로" />

        <TextView
            android:id="@+id/tv_screen_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:text="식당 등록"
            android:textColor="@color/text_primary"
            android:textSize="17sp"
            android:textStyle="bold" />
    </LinearLayout>

    <View
        android:id="@+id/divider_top"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="@color/divider"
        app:layout_constraintTop_toBottomOf="@+id/top_bar" />

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/divider_top">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="20dp">

            <!-- 상호명 -->
            <TextView style="@style/FormLabel" android:text="상호명 *" />
            <EditText
                android:id="@+id/et_name"
                style="@style/FormInput"
                android:hint="예: 백석 칼국수" />

            <!-- 카테고리 -->
            <TextView style="@style/FormLabel" android:text="카테고리 *" />
            <Spinner
                android:id="@+id/spinner_category"
                android:layout_width="match_parent"
                android:layout_height="48dp"
                android:layout_marginTop="6dp"
                android:layout_marginBottom="16dp" />

            <!-- 주소 -->
            <TextView style="@style/FormLabel" android:text="주소 *" />
            <EditText
                android:id="@+id/et_address"
                style="@style/FormInput"
                android:hint="예: 충남 천안시 동남구 ..." />

            <!-- 영업시간 -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal">

                <LinearLayout
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:orientation="vertical">
                    <TextView style="@style/FormLabel" android:text="오픈 시간 *" />
                    <TextView
                        android:id="@+id/tv_open_time"
                        style="@style/FormInput"
                        android:clickable="true"
                        android:focusable="true"
                        android:hint="시간 선택" />
                </LinearLayout>

                <View android:layout_width="12dp" android:layout_height="1dp" />

                <LinearLayout
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:orientation="vertical">
                    <TextView style="@style/FormLabel" android:text="마감 시간 *" />
                    <TextView
                        android:id="@+id/tv_close_time"
                        style="@style/FormInput"
                        android:clickable="true"
                        android:focusable="true"
                        android:hint="시간 선택" />
                </LinearLayout>
            </LinearLayout>

            <!-- 전화번호 -->
            <TextView style="@style/FormLabel" android:text="전화번호" />
            <EditText
                android:id="@+id/et_phone"
                style="@style/FormInput"
                android:hint="예: 041-123-4567"
                android:inputType="phone" />

            <!-- 휴무일 -->
            <TextView style="@style/FormLabel" android:text="휴무일" />
            <EditText
                android:id="@+id/et_closed_days"
                style="@style/FormInput"
                android:hint="예: 매주 월요일" />

            <!-- 설명 -->
            <TextView style="@style/FormLabel" android:text="가게 설명" />
            <EditText
                android:id="@+id/et_description"
                android:layout_width="match_parent"
                android:layout_height="100dp"
                android:layout_marginTop="6dp"
                android:layout_marginBottom="16dp"
                android:background="@drawable/bg_card_border"
                android:gravity="top|start"
                android:hint="가게를 소개해주세요"
                android:inputType="textMultiLine"
                android:padding="12dp"
                android:textColor="@color/text_primary"
                android:textColorHint="@color/text_hint"
                android:textSize="15sp" />

            <TextView
                android:id="@+id/btn_submit"
                android:layout_width="match_parent"
                android:layout_height="50dp"
                android:layout_marginTop="12dp"
                android:background="@drawable/bg_button_primary"
                android:clickable="true"
                android:focusable="true"
                android:gravity="center"
                android:text="등록"
                android:textColor="@color/white"
                android:textSize="15sp"
                android:textStyle="bold" />

        </LinearLayout>
    </ScrollView>

</androidx.constraintlayout.widget.ConstraintLayout>
